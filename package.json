{
  "name": "wedesign-deployer",
  "version": "1.0.0",
  "scripts": {
    "build": "mkdir -p source && tar -xf wedesign_code.tar.gz -C source && cd source && npm install && npm run build && cp -r dist ../../dist"
  },
  "dependencies": {
  }
}
